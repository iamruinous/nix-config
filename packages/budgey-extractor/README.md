# budgey-extractor

Offline OpenCode session extractor for Postgres + Weaviate.

## Notes

- Update `rev` and `hash` once the repo is tagged.
- `vendorHash` will need to be filled after running a nix build.

## Usage

Add `budgey-extractor` to `environment.systemPackages`.
