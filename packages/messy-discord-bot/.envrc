#!/usr/bin/env bash
# Use the nodejs devshell from this flake
source_up

# Load nodejs devshell
use flake ..#nodejs

# Watch devshell changes
watch_file ../../devshells/nodejs.nix

# Load local overrides if present
source_env_if_exists .envrc.local
