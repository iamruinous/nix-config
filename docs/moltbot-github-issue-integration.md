# Configuration Request: Openclaw GitHub/Forgejo Issue Integration

**Component:** Forgejo Tea CLI Authentication  
**Priority:** High  
**Type:** New Feature  
**Requested by:** openclaw (formerly moltbot)  
**Date:** 2026-01-29  
**Note:** Migrated from nix-moltbot fork to upstream nix-openclaw - see issue #391  

## Current State

- **GitHub (`gh` CLI):** ✅ Working - authenticated as iamruinous with repo/issues access
- **Forgejo (`tea` CLI):** ⚠️ Configured but authentication failing
  - Configured for forge.meskill.farm  
  - Error: "no gitea login detected, whether falling back to login 'forge.meskill.farm'? [y/N]"
  - CLI times out waiting for interactive input

## Required Configuration Changes

### 1. Forgejo/Tea CLI Authentication
```nix
# NixOS configuration needs:
# Method 1: Environment variables
environment.variables = {
  GITEA_LOGIN = "iamruinous";
  GITEA_URL = "https://forge.meskill.farm";
  GITEA_TOKEN = "<api_token>";  # Generated from forge.meskill.farm
};

# Method 2: Declarative config file
# ~/.config/tea/config.yml or system equivalent
```

### 2. API Token Requirements

**Forgejo API Token Needed:**
- **Location:** forge.meskill.farm → User Settings → Applications → Generate New Token
- **Required Scopes:**
  - `read:repository` - Read repo information
  - `write:issue` - Create, edit, comment on issues  
  - `read:user` - Basic user info access

### 3. Environment Variables for Multi-Platform Operations
```bash
# For seamless GitHub/Forgejo operations:
GITHUB_TOKEN="ghp_*"  # Already configured
FORGEJO_TOKEN="<new_token>"  # Needs generation
FORGEJO_DEFAULT_INSTANCE="forge.meskill.farm"
```

## Verification Steps

Once configured, verify with:
```bash
# Test Forgejo authentication
tea login list  # Should show authenticated session

# Test repository access  
tea repos list  # Should return repositories

# Test issue operations
tea issues list --repo <test-repo>  # Should list issues
tea issues create --title "Test Issue" --body "Authentication test" --repo <test-repo>
```

## Dependencies

- [ ] API token generation from forge.meskill.farm (requires human action)  
- [ ] NixOS configuration deployment
- [ ] Service restart/reload after configuration changes

## Implementation Notes

**NixOS Constraint:** All changes must be declarative in NixOS configuration. No manual CLI configuration that would be lost on next deployment.

**Testing Strategy:** After implementation, openclaw will verify all operations work across both GitHub and Forgejo platforms for full issue lifecycle management.

**Expected Capabilities After Implementation:**
- Create issues on both GitHub and Forgejo
- Read/list issues across both platforms  
- Comment and update issues programmatically
- Coordinate with other agents via issue assignments and mentions

---

*Configuration request generated by openclaw coordination system*